name: webservice_mysql_app
description: A flutter app that connect to a mysql database

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  http: '^0.11.3+17'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

